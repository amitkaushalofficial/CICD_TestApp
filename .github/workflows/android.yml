name: Android CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: set up JDK 12
        uses: actions/setup-java@v3
        with:
          java-version: '12'
      - name: Build with Gradle
        run: ./gradlew build